"""Root seeds and scenario fixtures for the decomposition harness.

Roots (``seeds/roots/*.md``) describe the initial intent + criteria of a run.
Events (``seeds/events/*.json``) are scheduled injections (user input, worker
fail/fill) that fire mid-run against a named root.
"""
